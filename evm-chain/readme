# JERT Permissioned EVM Chain

This module contains configuration files and scripts for running the JERT
Permissioned EVM network (IBFT/PoA).

- `config/genesis.json` – chain genesis configuration
- `config/static-nodes.json` – validator node list
- `scripts/init-network.sh` – helper script to bootstrap validators
