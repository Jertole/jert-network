
{
  "name": "jert-smart-contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile && ts-node scripts/export-abi.ts",
    "test": "hardhat test"
      }
}

    "deploy": "hardhat run scripts/deploy-all.ts --network jert"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.22.0",
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.2",
    "@types/dotenv": "^8.2.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.4.5"
  }
}
